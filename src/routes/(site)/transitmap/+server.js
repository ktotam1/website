import Papa from 'papaparse';
import { connectToDB, query } from '$lib/server/db';

export const POST = async (event) => {
    // connectToDB()
    // let res = await query("SELECT * FROM a")
    // console.log("*****************************\n", res)
    return new Response()
}